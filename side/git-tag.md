git-tag ⟜ create annotated version tag

Tag a release with version and message for publishing.

◊ ⊢ create annotated git tag ⊣
  ⟜ git tag -a v[version] -m "[message]"
  ⟜ verify tag created: git tag -l v[version]
  ⟜ ready for push to remote

◉ version tag created
