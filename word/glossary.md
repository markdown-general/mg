◊ glossary

Edges and elaborations from the memory-map, shaped by agent work.

---

**elaborate** ⟜ agents and cards teach each other what clarity looks like

Uses: elaborate → card ↔ agent (bidirectional) → semantics/syntax working toward shared meaning.

**compose** ⟜ intent becomes traceable when flow marks the shape of thinking

Uses: compose → intent → flow (together they mark topology); flow → context means movement builds understanding.

**collaborate** ⟜ multiplicity discovers what works through shared space

Uses: collaborate → multiplicity → space (where sharing → space carries the bidirectionality of finding common ground).
