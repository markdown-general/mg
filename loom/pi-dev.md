https://pi.dev/packages 

⟜ MCP, extensions and packages are imperative and don't seem to mix well with mg cards. mg is functional - cards should compose (not contradict each other and confuse in combinations).

⟜ We would like to find a way to use extensions but remove the imperative language in the prompts. Before we do that work, however, we need to understand the tool and skills used. We need to use them.

⟜ This is our first real experience with MCP, extensions and packages. We currently use zero of these.

⟜ the engineering we need right now is to remain agnostic to how everything runs. A system that can be torn down andrebuilt quickly, and that doesn't leave bits of state and configuration everywhere.

⟜ that's the constraint. Beyond this we are looking to

⟝ upgrade skills in multi-agent orchestration 
⟝ provide agents with an ability to use interactive tools (pi is the prime example but cabal repl also as an alternative - anything that need stdin/stdout/stderr)
⟝ category theory skills
⟝ Functional and Haskell coding skills
⟝ improve our capabailities as much as we can as fast as we can
⟝ Find out what others do.

https://www.npmjs.com/package/pi-agent-browser-native
